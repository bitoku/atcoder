#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef long double ld;
const ll mod = 1000000007;

int main() {
    ll t;
    cin >> t;
    for (int i = 0; i < t; ++i) {
        ll n;
        cin >> n;
        string s;
        cin >> s;
        cin >> s;
        cin >> s;
        cout << string(n, '1') << string(n, '0') << '1' << endl;
    }
}
