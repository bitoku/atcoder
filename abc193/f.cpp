#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const ll mod = 1000000007;

int main() {
    int n;
    cin >> n;
    vector<string> c(n);
    for (int i = 0; i < n; ++i) {
        cin >> c[i];
    }

}
